# JavaScript Fundamentals – Classroom Template (Full Export)

This repository contains 22 chapter folders under `exercises/`, each with a starter JS file, a Jest test, and a brief README.
Run:

```bash
npm ci
npm test
# or a specific chapter
npm test exercises/08-variables
```

Uses Node 20, Jest, ESLint (standard), and Prettier.
